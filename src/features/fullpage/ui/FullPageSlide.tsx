'use client';

import type { BackgroundType } from '@/shared/ui';

export type FullPageSlideProps = {
	children: React.ReactNode;
	id: string;
	bgVariant?: BackgroundType;
};

export function FullPageSlide(_props: FullPageSlideProps): null {
	return null;
}
